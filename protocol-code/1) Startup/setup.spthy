theory setup
begin

/*
 * SECTION: Start-up
 *
 * Bare-minimum: just Client, Server, and the top-level process block.
 */

let Client() =
    0

let Server() =
    0

process:
( !Client() | !Server() )

end
