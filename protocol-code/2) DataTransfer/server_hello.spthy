theory server_hello
begin

let Server =
    new sr;
    event Create_ServerHello(sr);
    out(sr)

let Client =
    in(x);
    event ClientReceivedHello(x)

process:
    ( !Server ) | ( !Client )

lemma exists_ServerHello:
exists-trace
  " Ex x #i. ClientReceivedHello(x) @ i "

end
